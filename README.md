# LLM Confidence

## Usage

Please add your hugging face and groq tokens to a file named `config.yaml` and place it under `src` directory

Sample `config.yaml`
```yaml
huggingface:
  token: <YOUR_HUGGING_FACE_TOKEN>
groq:
  tokens:
    - <YOUR_GROQ_TOKEN>
```